package gui;

import java.awt.GridLayout;
import java.util.Iterator;

import javax.swing.JComponent;
import javax.swing.JLabel;


import insanlar.Musteri;
import main.Restoran;

public class KasaMenu {
	/*

JLabel[] kasaSirasi;
	
	public KasaMenu(JComponent parent, String name, Restoran restoran) {
		super(parent, name, restoran);
		kasaSirasi = new JLabel[3];
		
		this.setLayout(new GridLayout(1, 3));
		
		for(int i = 0; i < 3 ; i++) {
			kasaSirasi[i] = new JLabel();
			this.add(kasaSirasi[i]);
		}
		
	}
	
	
	public void updateLabels() {
		int i = 0;
		for(Iterator<Musteri> it = restoran.getKasaSirasi().iterator(); i < 3; i++) {
			if(it.hasNext()) {
				Musteri musteri = it.next();
				if(musteri == null) break;
				
				kasaSirasi[i].setText("Musteri " + musteri.getID());
			}
			
		}
	}
	*/
	
}
